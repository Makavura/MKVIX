language: python
python:
 - "2.7"

install:
  - pip install -r requirements.txt
  - pip install coveralls
  - pip install pytest-cov

script: 
  - pytest --cov test_app.py

after_success:
  - coveralls

after_script:
  - coveralls
  
